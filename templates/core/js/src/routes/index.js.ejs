import { Router } from 'express';
import healthRoutes from './healthRoutes.js';
<% if (features.includes('auth')) { %>import authRoutes from './authRoutes.js';<% } %>

const router = Router();

router.use('/health', healthRoutes);
<% if (features.includes('auth')) { %>router.use('/auth', authRoutes);<% } %>

export default router;
